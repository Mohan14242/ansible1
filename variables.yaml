- name: usging the tags in asnible 
  hosts: localhost
  become: yes
  tasks:
  - name: for oringtin the 
    ansible.builtin.yum:
      name: "nginx"
      state: installed
    tags:
      - mohan
  - name: for pravven
    ansible.builtin.yum:
      name: "nginx"
      state: abscent
    tags: 
      - praveen