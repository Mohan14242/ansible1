- name: usging the tags in asnible 
  hosts: localhost
  become: yes
  vars:
  - name: mohan
  - age: 25
  tasks:
  - name: for oringtin the 
    ansible.builtin.yum:
      name: "nginx"
      state: installed
    tags:
      - mohan
      - chiru
  - name: for pravven
    ansible.builtin.yum:
      name: "nginx"
      state: absent
    tags: 
      - praveen
  - name: copy the mosules
    ansible.buitlin.copy:
      src: template/mohan.j2
      dest: /tmp/